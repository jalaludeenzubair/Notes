| **Problem Name**                            | **Deep Intuition (With Example)**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Two Sum**                                 | Think of this like shopping: you know the final bill (target) and every price you see makes you check, “What price would pair with this one to hit the exact bill?” That missing price is `target – price`. Keep a map of prices you’ve already seen so as soon as you find a match, you're done. <br> **Example:** target = 9, nums = [2, 7, 11, 15]. See `2`, store it. See `7`, check: `9 - 7 = 2` → already seen → answer.                                                                                      |
| **Best Time to Buy and Sell Stock**         | Imagine you're walking through stock prices while always asking: _“What’s the lowest price I’ve seen before this moment?”_. Every new price is a chance to sell. The profit is today’s price minus the minimum so far. Update the minimum whenever you see a lower price. <br> **Example:** prices = [7,1,5,3,6,4]. Lowest so far becomes 1. Best sell happens at 6 → profit = 5.                                                                                                                                   |
| **Contains Duplicate**                      | You're scanning a guest list. Every time you see a name, you check a record of names you’ve seen before. If it’s already in the record, someone entered twice. <br> **Example:** [1,2,3,1] → when you see the last `1`, it's already in the set → duplicate.                                                                                                                                                                                                                                                        |
| **Contains Duplicate II**                   | Think of a sliding “radius of memory” of size k. You only care if duplicates occur within k steps. Maintain a moving window that always contains the last k values. <br> **Example:** nums=[1,2,3,1], k=3 → index difference between both 1’s is 3 → return true.                                                                                                                                                                                                                                                   |
| **Product of Array Except Self**            | The product at each position is everything to the left × everything to the right. Build left products first, then multiply them by right products on the second pass without using extra space. <br> **Example:** [1,2,3,4] → left = [1,1,2,6], right pass gives final [24,12,8,6].                                                                                                                                                                                                                                 |
| **Maximum Subarray (Kadane’s)**             | You grow a running subarray sum, but if it becomes negative, you discard it because a negative sum only hurts future totals. You're building the best streak. <br> **Example:** [-2,1,-3,4,-1,2,1,-5,4]. Best streak is [4, -1, 2, 1] → sum = 6.                                                                                                                                                                                                                                                                    |
| **Maximum Product Subarray**                | Products flip sign when multiplied by negatives, so track _both_ the biggest and smallest product up to each point because the smallest (negative) value can become the biggest after multiplication by a negative number. <br> **Example:** nums = [2,3,-2,4]. At `-2`, max and min swap roles → final max = 6.<br><br><br><br> See the answer always going to be if we multiply all number until we dont have negative on our array so in that case only we need to skip that so keep we need to track min so far |
| **Find Minimum in Rotated Sorted Array**    | Rotated arrays behave like two sorted arrays glued together. The minimum is the "pivot" where the order breaks. Binary search looks for the unsorted half because the pivot must lie there. <br> **Example:** [4,5,6,7,0,1,2] → right side is unsorted → pivot must be there → min = 0.                                                                                                                                                                                                                             |
| **Search in Rotated Sorted Array**          | The array has one sorted half and one rotated half. Find which half is sorted first. If the target lies inside that sorted half, search there; otherwise, go to the other half. <br> **Example:** nums=[4,5,6,7,0,1,2], target=0 → right side sorted? No. Left sorted? Yes. But target not in [4..7] → go right → found.                                                                                                                                                                                            |
| **Two Sum II (Sorted)**                     | Two pointers behave like squeezing tongs: if the sum is too small, raise the lower end; if too big, lower the high end. The sorted order ensures movement is always correct. <br> **Example:** [2,7,11,15], target=9 → 2+15 too big → move right inward → 2+7=9.                                                                                                                                                                                                                                                    |
| **Three Sum**                               | After sorting, fix one number and shrink the remaining search space like Two-Sum-II. Skip duplicates at all three roles (i, left, right). <br> **Example:** [-1,0,1,2,-1,-4] → sorted → [-4,-1,-1,0,1,2]. Fix -1, find (0,1).                                                                                                                                                                                                                                                                                       |
| **Merge Sorted Array**                      | Since nums1 has space at the end, fill it backwards from largest to smallest. This avoids overwriting useful data. <br> **Example:** nums1=[1,2,3,*,*,_], nums2=[2,5,6] → start from end and place 6, 5, 3, etc.                                                                                                                                                                                                                                                                                                    |
| **Container With Most Water**               | The width is biggest at the edges. The height is always limited by the _shorter_ side. Move only the shorter side inward hoping to find a taller boundary. Moving the taller side can never help. <br> **Example:** heights=[1,8,6,2,5,4,8,3,7]. Best area occurs at 8 and 7 with width 7.                                                                                                                                                                                                                          |
| **Verifying an Alien Dictionary**           | You just map each alien letter to its rank. Then compare each pair of adjacent words like comparing words in English. As soon as you find a mismatching character, compare by alien ranks. <br> **Example:** order="hlabcdefgijkmnopqrstuvwxyz", words=["hello","leetcode"] → compare h vs l → valid.                                                                                                                                                                                                               |
| **Next Permutation**                        | You're looking for the next lexicographically larger arrangement. Move from right until you find a "rise" (pivot). Swap with the next-larger element to the right, then reverse everything after pivot (because it's currently in descending order). <br> **Example:** [1,2,3] → next is [1,3,2].                                                                                                                                                                                                                   |
| **Remove Duplicates from Sorted Array**     | Move a slow pointer marking where the next unique should go. Walk with a fast pointer; whenever you find a new unique value, write it at the slow pointer. Sorted order ensures duplicates are always adjacent. <br> **Example:** [1,1,2] → result becomes [1,2].                                                                                                                                                                                                                                                   |
| **Find First and Last Position of Element** | Perform binary search twice: one forces the search left to find the earliest occurrence, the other forces right to find the latest. <br> **Example:** nums=[5,7,7,8,8,10], target=8 → left boundary=3, right=4.                                                                                                                                                                                                                                                                                                     |
| **Trapping Rain Water**                     | Water at each position depends on the tallest wall on the left and right. Using two pointers, always move the pointer with the lower wall because that side limits water height. <br> **Example:** [4,2,0,3,2,5] → total trapped = 9.                                                                                                                                                                                                                                                                               |
| **Median of Two Sorted Arrays**             | Imagine splitting two arrays into left and right halves where every left element ≤ every right element. Binary search finds the correct split in the smaller array. Once the partition is correct, reading the median is direct. <br> **Example:** A=[1,3], B=[2] → left side is [1,2], right is [3] → median = 2.                                                                                                                                                                                                  |
| **Valid Anagram**                           | Track character counts: add for string s, subtract for string t. If everything cancels to zero, they match like perfect ingredient lists. <br> **Example:** s="anagram", t="nagaram".                                                                                                                                                                                                                                                                                                                               |
| **Top K Frequent Elements**                 | First count frequencies. Then maintain a min-heap of size k containing the most frequent elements so far. If the heap grows too large, remove the smallest frequency. <br> **Example:** [1,1,1,2,2,3], k=2 → top 2 = [1,2].                                                                                                                                                                                                                                                                                         |
| **Group Anagrams**                          | Anagrams share identical letter counts. Use a 26-length character frequency tuple as a signature. Group strings by that signature. <br> **Example:** [“eat”,”tea”,”tan”,”ate”,”nat”,”bat”] → groups: [eat,tea,ate], [tan,nat], [bat].                                                                                                                                                                                                                                                                               |
| **Valid Sudoku**                            | Maintain sets for rows, columns, and 3×3 boxes. If a digit appears twice in any of these, the board becomes invalid. <br> **Example:** Two 5’s in the same row → invalid.                                                                                                                                                                                                                                                                                                                                           |
| **Encode and Decode Strings**               | Use a reliable method to store string lengths so decoding is unambiguous. Common approach: `"length#string"`. On decode, read until `#`, extract length, then extract exactly that many chars. <br> **Example:** ["lint","code"] → "4#lint4#code".                                                                                                                                                                                                                                                                  |
| **Longest Consecutive Sequence**            | Put all numbers in a set, but only start counting a sequence from numbers that are true _starts_ (i.e., n-1 is not in the set). This prevents repeatedly scanning the same chain. <br> **Example:** [100,4,200,1,3,2] → start at 1 → sequence = 1,2,3,4 → length = 4.                                                                                                                                                                                                                                               |

| Problem Name                                      | Intuition Depth and Example                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| :------------------------------------------------ | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Longest Common Prefix**                         | The core intuition is that the longest common prefix (LCP) can never be longer than the shortest string in the array; therefore, we initially assume the **first string is the LCP**. We then iteratively compare this candidate prefix against all other strings in the array. If a string does not contain the current candidate prefix at the start, we **shorten the candidate prefix by one character** from the end and repeat the check until it matches or the prefix becomes an empty string. **Example:** Given `["flower", "flow", "flight"]`. Start with `prefix = "flower"`. Compare with "flow". Mismatch after 'w'. Shorten to `"flow"`. Compare `"flow"` with `"flight"`. Mismatch after 'l'. Shorten to `"flo"`. Still mismatch. Shorten to `"fl"`. Match. LCP is `"fl"`.                                                                                                                                                            |
| **FizzBuzz**                                      | This problem uses looping and conditional logic, but requires the output of results as strings. The primary intuition is to prioritize the most restrictive condition first: check for divisibility by 3 and 5. If a number is divisible by both (e.g., 15), return "FizzBuzz". Subsequent checks handle single factors (divisible by 3 -> "Fizz"; divisible by 5 -> "Buzz"). If none apply, the number itself is converted to a string and returned. **Example:** For input $N=5$. At 3, it is divisible by 3 only, output is "Fizz". At 5, it is divisible by 5 only, output is "Buzz".                                                                                                                                                                                                                                                                                                                                                             |
| **Longest Repeating Character Replacement**       | This uses the **Sliding Window** technique, relying on the insight that the length of a valid substring is constrained by the maximum frequency of a single character (the character we choose to replace all others with) and the allowed number of replacements ($K$). The key formula for a window to be valid is: **(Window Length) - (Max Occurrence in Window) $\le K$**. We expand the Right pointer. If the window becomes invalid (differs by more than $K$ from the max occurring character), we shrink the Left pointer until the condition is met again. **Example:** $S = \text{"AABA A"}$, $K=1$. Window "AAB" (Length 3, Max A=2). $3-2=1$. Valid. Window "AABA" (Length 4, Max A=3). $4-3=1$. Valid. Max length is 4.                                                                                                                                                                                                                 |
| **Longest Substring Without Repeating Character** | This uses the **Sliding Window** approach coupled with a **Hash Set** to enforce the uniqueness constraint. The Right pointer expands the window. The Hash Set provides $O(1)$ lookups to check for duplicates. If the Right pointer encounters a character already in the Hash Set, the Left pointer must advance forward, removing characters from the start of the window (and the set), until the duplicate character is removed and uniqueness is restored. The maximum length found during expansion is tracked. **Example:** Given $S = \text{"AB C A B}$" (L starts at A, R expands). When R hits the second 'A', the 'A' is already in the set. L must slide forward, removing the first 'A'. The window shrinks to "BCA", then R continues expanding.                                                                                                                                                                                       |
| **Minimum Window Substring**                      | This is a sophisticated **Sliding Window** problem requiring two **Hash Maps** and two counters (`Required` and `Create`). One map tracks the required character counts from string $T$ (`Map T`). The second map tracks counts in the current window in $S$ (`Substring Hashmap`). The Right pointer expands the window until the count of unique characters satisfying the frequency requirement (`Create`) equals the total unique characters needed from $T$ (`Required`). Once this condition is met, the Left pointer is moved inward to shrink the window and find the minimum length, tracking when the required counts are potentially broken. **Example:** $S = \text{"ADOBECODEBANC"}$, $T = \text{"ABC"}$. Expand R until A, B, C are found (e.g., in "ADOBEC"). Once found, shrink L from 'A'. If L removes 'D' (unnecessary), the window shrinks. If L removes a necessary character (like 'A'), R must expand again to find a new 'A'. |
| **Valid Anagram**                                 | The fundamental insight is that anagrams must have the exact same count and types of characters, regardless of order. We use a **fixed-size array (size 26) as a counter** for characters. Iterate over string $S$, **incrementing** the count for each character. Iterate over string $T$ (or iterate both simultaneously), **decrementing** the count for each character. If the strings are anagrams, the final count array must have all entries equal to zero. **Example:** $S = \text{"cat"}$, $T = \text{"act"}$. Count C+1, A+1, T+1. Then count A-1, C-1, T-1. All indices return to zero, confirming they are anagrams.                                                                                                                                                                                                                                                                                                                     |
| **Group Anagrams**                                | To group anagrams, we need a way to assign a **canonical key** that is identical for all anagrams but unique otherwise. The method uses a **Hash Map** where the key is a string derived from the character count structure of the word (e.g., an array of 26 counts converted to a string). Iterate through the input list; for each word, generate this unique count-key and append the original word to the list stored under that key in the hash map. **Example:** "eat", "tea", and "ate" all have the composition (A: 1, E: 1, T: 1). This count is converted into a unique string key. The hash map stores this key, and the value is the list `["eat", "tea", "ate"]`.                                                                                                                                                                                                                                                                       |
| **Valid Parentheses**                             | This problem requires checking both the **matching type** of brackets and the **correct order** of closing. A **Stack** is used to maintain the order of _opening_ brackets encountered. A **Hash Map** is used to quickly look up the required _opening_ bracket for any given _closing_ bracket. When an opening bracket is encountered, it is pushed onto the stack. When a closing bracket arrives, the corresponding opening bracket is popped from the stack and compared to the expected match from the hash map. If they don't match, or if the stack is empty when a closing bracket arrives, it is invalid. **Example:** For `( [ ] )`. Push `(`. Push `[`. Encounter `]`. Pop `[`. Match. Encounter `)`. Pop `(`. Match. Stack is empty, thus valid.                                                                                                                                                                                       |
| **Valid Palindrome**                              | This uses the **Two Pointers** technique to compare characters from the outside inward, optimizing for space by avoiding the creation of a reversed string. Crucially, the algorithm must skip non-alpha numeric characters and normalize characters to lowercase before comparison. Left pointer moves right, Right pointer moves left. The comparison only happens when both L and R point to valid, normalized characters. **Example:** For $S = \text{"M ad a M}$". L starts at M, R starts at M. Both are valid. Compare M and M. Match. L moves to 'a', R moves to space. R skips space and moves to 'a'. Compare 'a' and 'a'. Match. Continue until the pointers cross.                                                                                                                                                                                                                                                                        |
| **Longest Palindromic Substring**                 | The intuition is that palindromes expand symmetrically around a **center**. We check two types of centers for every index $I$: **Odd length** palindromes (center at $I$) and **Even length** palindromes (center between $I$ and $I+1$). The algorithm **expands around the center** by checking if the character at $L$ equals the character at $R$, moving $L$ leftward and $R$ rightward as long as they match and remain in bounds. We track the maximum length found across all possible centers. **Example (Odd):** Starting at center 'D' in "MADAM", expand L and R. 'A' matches 'A'. Expand again. 'M' matches 'M'. Longest palindrome found is "MADAM".                                                                                                                                                                                                                                                                                    |
| **Palindromic Substrings**                        | Similar to finding the longest substring, this method uses the **Expand Around Center** technique. However, the goal is to **count** all palindromes, not just find the longest one. Every single character is a palindrome, and every successful step of expansion (where L and R characters match) generates a new, longer palindrome that must be counted. We check both odd-length (single center $I$) and even-length (dual center $I, I+1$) cases for every index. **Example:** For "CABA". Check 'C' (Count 1). Check 'A' (Count 1). Check 'B': expand around 'B' (L='A', R='A'), new match ("ABA", Count 1). Total count is $1+1+1+1+1 = 5$ palindromes.                                                                                                                                                                                                                                                                                      |
| **Encode and Decode Strings**                     | This solves the problem of transmitting a list of strings as a single continuous string. The intuition is to use a unique **delimiter or separator character** that is guaranteed not to be in any of the input strings (e.g., character 257 for ASCII strings). **Encoding** involves concatenating all strings and inserting this unique delimiter after each one. **Decoding** involves splitting the single encoded string based on this delimiter. **Example:** Encode `["hello", "world"]` using delimiter '#'. Encoded string becomes `"hello#world#"` (or `"hello#world"`). Decode by splitting on `#` to retrieve the original list.                                                                                                                                                                                                                                                                                                         |
| **Letter Combinations of a Phone Number**         | This combines **Recursion and Backtracking** to generate all possible permutations. First, a mapping is established between digits (2-9) and their corresponding letters. The recursive function explores all possible "paths" in the decision tree structure, where each level of recursion corresponds to processing the next digit in the input. Backtracking ensures that after one combination is formed (e.g., 'AD'), the function returns to the previous state (A) to explore the next option (A to E). The base case for the recursion is reached when the current combination length equals the total input digit length. **Example:** Input "23" (2 $\to$ ABC, 3 $\to$ DEF). The process recursively builds combinations: 2(A) $\to$ 3(D) $\to$ "AD". Backtrack. 2(A) $\to$ 3(E) $\to$ "AE". Backtrack. 2(A) $\to$ 3(F) $\to$ "AF". Then backtrack to 2(B) and repeat.                                                                     |
| **Text Justification**                            | The approach is **Greedy**, aiming to fit the maximum number of words possible into a line (`Max Width`). The main challenge is the calculation and distribution of required spaces. For each line (except the last), we calculate the total available space needed to reach `Max Width`, and divide this space among the gaps between words. Any remainder of spaces must be distributed **evenly, prioritizing the left gaps**. The last line is handled differently, requiring only left justification with a single space between words. **Example:** Max Width 10. Words "a", "b", "c", "d". If we pack "a", "b", "c" (length 3). Required spaces = $10 - 3 = 7$. Two gaps (a/b and b/c). $7/2 = 3$ spaces with a remainder of 1. The first gap gets $3+1=4$ spaces, the second gets $3$ spaces. Result: "a b c".                                                                                                                                |

| Problem Name                                              | Intuition Depth and Example                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| :-------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Daily Temperatures** (Finding the Immediate Warmer Day) | This problem leverages the stack's LIFO property to efficiently find the **immediate greater element** to the right for every item. Instead of storing temperatures, the **indices** of the days are stored in the stack. We iterate through the temperature array. If the current day's temperature is _warmer_ than the temperature index currently at the top of the stack, that stack index is popped. The answer for the popped index is calculated as the difference between the current index ($I$) and the popped index. This is repeated until the stack top is warmer or the stack is empty. This guarantees that only the _immediate_ next warmer day is counted. **Example:** Temperatures ``. Stack stores index `0` (temp 73). At index 1 (temp 74), 74 > 73 (stack top). Pop 0. Answer for index 0 is $1 - 0 = 1$. Push 1. At index 2 (temp 75), 75 > 74. Pop 1. Answer for index 1 is $2 - 1 = 1$. Push 2. At index 3 (temp 71), 71 is _less_ than 75. Push 3.                           |
| **Evaluate Reverse Polish Notation**                      | This uses a stack to manage the order of operands in arithmetic expressions written in RPN (postfix) form. The intuition is simple: numbers are pushed onto the stack. When an operator (+, -, _, /) is encountered, the LIFO property ensures that the two most recent operands (A and B) are immediately available by popping them. The operation is performed (treating the first popped item as B and the second as A), and the resulting value is pushed back onto the stack, replacing the operands. The final answer is the last remaining value in the stack. **Example:** Tokens `[2, 3, 7, + , _]`. Push 2, 3, 7. See '+'. Pop 7 (B), Pop 3 (A). Calculate $3 + 7 = 10$. Push 10. Stack: ``. See '_'. Pop 10 (B), Pop 2 (A). Calculate $2 _ 10 = 20$. Push 20. Final answer is 20.                                                                                                                                                                                                             |
| **Min Stack**                                             | The challenge is providing $O(1)$ time complexity for finding the minimum element (`getMin`) while still maintaining standard stack functionality. The optimal intuition is to modify the data storage so that **every single element** pushed onto the stack carries the historical minimum value seen _up to that point_. This can be achieved by using a Node structure containing the current `value`, the `Min value` so far, and a reference to the `next` node. Alternatively, two stacks can be used in parallel: one for the data and one for tracking the running minimum. **Example (Node approach):** Push 3. Node (Value: 3, Min: 3). Push 5. Compare 5 and previous Min (3). Node (Value: 5, Min: 3). Push 1. Compare 1 and previous Min (3). Node (Value: 1, Min: 1). Now, if 1 is popped, the stack immediately exposes the minimum of the remaining elements (3).                                                                                                                       |
| **Max Stack**                                             | This extends the basic stack by requiring $O(1)$ operations for `peakMax` and `popMax`. The solution uses **two parallel stacks**: a regular stack for data and a `Max Stack`. The `Max Stack` always tracks the maximum value seen up to that point in the primary stack. `peakMax` simply checks the top of the `Max Stack`. `popMax` is the complex operation: it requires buffering (temporarily moving) all elements from the regular stack to a buffer stack until the target maximum element is reached and popped, and then moving the buffered elements back. **Example:** Push 1, 6, 3, 8. Regular Stack: `. Max Stack:`. `peakMax` returns 8. If we want `popMax` (which is 8), we pop 8 from both stacks. If we try to `popMax` again, the max is now 6. We must pop 3 from the regular stack into a buffer, pop 6 from both, then push 3 back from the buffer.                                                                                                                              |
| **Generate Parenthesis**                                  | This uses **Backtracking (or recursion)**, which relies on the implicit LIFO structure of the call stack. The intuition establishes strict rules to ensure valid parenthesis generation: 1) The count of open brackets must not exceed $N$. 2) A closing bracket can only be added if the current number of open brackets is _greater_ than the current number of closing brackets, ensuring proper pairing and order. The function recursively explores paths until both open and closed counts equal $N$. **Example ($N=2$):** Start: `(`. Open=1, Closed=0. Option 1: Open again $\to$ `((`. Open=2, Closed=0. Option 2: Close $\to$ `()`. Open=1, Closed=1. From `((`, only close is possible $\to$ `()`. Open=2, Closed=1. Then close $\to$ `(())`. Valid. From `()`, option to open $\to$ `()(`. Open=2, Closed=1. Then close $\to$ `()()`. Valid.                                                                                                                                                 |
| **Minimum Remove to Make Valid Parenthesis**              | A stack is used to identify **unmatched parentheses indices**. The LIFO property tracks open parentheses waiting for a match. Iterate through the string: if an opening bracket is found, push its index onto the stack. If a closing bracket is found, check the stack: if the stack is _not_ empty, pop the corresponding opening index (they form a valid pair). If the stack _is_ empty when a closing bracket arrives, that closing bracket index is **invalid** and stored in a hash set for removal. After the loop, any indices left in the stack correspond to **unmatched open brackets** (which are also invalid). The final string is then built by excluding all indices marked in the hash set. **Example:** String `(a) )`. Index 0 `(`: push 0. Index 2 `)`: pop 0. Stack empty. Index 4 `)`: Stack is empty, so this `)` is invalid. Store index 4. Result: Remove character at index 4.                                                                                                |
| **Valid Parentheses**                                     | This is the classic stack application, requiring checks for correct **matching type** and **correct order**. Opening brackets are pushed onto the stack. A hash map is used to pair closing brackets to their expected opening counterparts. When a closing bracket arrives, the element at the top of the stack is popped. If the popped element does not match the required opening bracket type (based on the hash map), the string is invalid. If the stack is empty when a closing bracket is encountered, the string is invalid. A valid string must result in an empty stack after processing. **Example:** String `({)}`. Push `(`, then `{`. Encounter `)`. Pop `{`. Expected match for `)` is `(`. Mismatch! Invalid.                                                                                                                                                                                                                                                                          |
| **Largest Rectangle in a Histogram**                      | This uses a **Monotonic Stack** containing indices, where the corresponding bar heights are kept in **ascending order**. The stack's LIFO property helps quickly find the left boundary of a potential rectangle. Iterate through the bars: if the current height is _less_ than the height corresponding to the stack top, it means the stack top bar's potential width is limited. Pop the stack top index. Calculate the area using the popped bar's height. The width is calculated using the difference between the current index ($I$) and the index now at the stack top (which serves as the left boundary). This ensures that the maximum area for every popped bar is calculated the moment its right boundary (a lower bar) is found. **Example:** Heights ``. Push index 0 (height 2). At index 1 (height 1): 1 < 2. Pop index 0 (height 2). Area = $2 \times (1 - \text{StackTop} - 1)$. Track max area. Push index 1.                                                                      |
| **Longest Valid Parenthesis**                             | The stack is used to track the **base index** or starting points for measuring valid lengths. The stack is initialized with index **-1** to handle zero-based indexing easily. Open brackets indices are pushed. When a closing bracket is encountered, the element is popped. If the stack is _not_ empty after the pop, the current valid length is calculated by subtracting the index at the stack top (the remaining unmatched open boundary) from the current index ($I$ minus $\text{Stack.Peak()}$). If the stack _is_ empty after the pop, it means the closing bracket is unmatched to the left, so the current index $I$ is pushed as the new base/starting point for measuring the _next_ potential valid substring. **Example:** String `( ) ( )`. Stack starts `[-1]`. Index 0 `(`: Push 0. Index 1 `)`: Pop 0. Stack: `[-1]`. Not empty. Length $1 - (-1) = 2$. Max length = 2. Index 2 `(`: Push 2. Index 3 `)`: Pop 2. Stack: `[-1]`. Not empty. Length $3 - (-1) = 4$. Max length = 4. |
